<Shell
    x:Class="UserManagementApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:UserManagementApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="UserManagementApp">

    <ShellContent Title="Login" Route="LoginPage" ContentTemplate="{DataTemplate views:LoginPage}" />
    <ShellContent Title="Register" Route="RegisterPage" ContentTemplate="{DataTemplate views:RegisterPage}" />
    <ShellContent Title="Userprofile" Route="UserprofilePage" ContentTemplate="{DataTemplate views:UserprofilePage}" />

    <!-- Add these routes -->
    <TabBar>
        <ShellContent Title="Home" Route="HomePage" ContentTemplate="{DataTemplate views:HomePage}" />
        <ShellContent Title="Profile" Route="ProfilePage" ContentTemplate="{DataTemplate views:ProfilePage}" />
    </TabBar>

</Shell>